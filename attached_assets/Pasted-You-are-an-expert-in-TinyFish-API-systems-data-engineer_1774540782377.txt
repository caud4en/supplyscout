You are an expert in TinyFish API systems, data engineering, and production-grade data validation pipelines.

The current system is producing fake or unverified companies and websites, which is unacceptable. This must be fully eliminated.

Your task is to completely rebuild both the development and production databases such that every single company and website is verifiable, and legitimate, with zero tolerance for fabricated or unverified data.

Critical Requirements (Non-Negotiable)
DO NOT fabricate any company or website under any circumstance
ONLY include entries that can be independently verified as real
Any entry that fails validation must be excluded
These rules must be strictly enforced with zero exceptions
Objectives
Full Database Reset
Safely and completely remove all existing entries from both development and production databases
Preserve schema, structure, and integration points with the TinyFish pipeline
Strict Data Verification Framework
Design a multi-layer validation system to ensure every company and website is real:
Domain validation (valid DNS resolution)
HTTP(S) verification (successful response, valid SSL)
Content validation (non-placeholder, relevant company information)
Optional: cross-referencing with trusted sources (e.g., official registries, reputable directories, APIs)
Trusted Data Sourcing
Only retrieve data from reputable and authoritative sources
Avoid low-quality, unverified, or autogenerated datasets
Dataset Reconstruction
Rebuild the dataset from scratch while:
Maintaining the same required dataset size (≥10,000 rows or previously defined total)
Ensuring 100% of entries pass all validation checks
Preventing duplicates and inconsistent records
Environment Consistency
Ensure both development and production databases:
Contain identical, fully verified datasets after reconstruction
Remain synchronized and consistent
Pipeline Integration
Ensure the verified dataset integrates seamlessly into the existing RAG / TinyFish pipeline
Maintain compatibility with embeddings, indexing, and retrieval workflows
Ongoing Data Integrity
Implement safeguards to prevent future invalid entries:
Pre-insertion validation gates
Periodic re-validation of stored websites
Logging and alerting for failures
Constraints
Maintain all previously defined requirements, including:
Dataset size (must remain constant as before)
Persistent, append-only behavior after reconstruction
Compatibility with existing system architecture
Ensure no invalid, fake, or unverifiable data enters the system at any point
The process must be deterministic, auditable, and reproducible
Output Requirements
Provide:
A high-level architecture for strict data validation and ingestion
A step-by-step execution plan for database reset and reconstruction
Detailed validation logic and criteria
Suggested tools, APIs, and verification techniques
Key code snippets or modules
A monitoring and enforcement strategy to guarantee ongoing data integrity